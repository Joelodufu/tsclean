export function parseArgs(argv: string[]) {
  // Parse argv and return a structured object:
  // { command: 'feature' | 'create', projectName, path, features: [{name, fields}], ... }
  // Implement parsing logic here
}
